#include "main.h"

/**
 * _isupper - function checks for uppercase character
 * @c: the character set to be checked
 * Return: 1 if c is uppercase, 0 otherwise
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
