#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _strlen - returns the lenth of a string
 *
 * @s: first parameter.
 *
 * Return: length of string.
 */

int _strlen(char *s)
{
	int i;
	*s = s[i];

	for (i = 0; s[i] != '\0'; ++i)
	{
		printf("%d", i);
		return (0);
	}
}
