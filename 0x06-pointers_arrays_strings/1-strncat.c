#include "main.h"
#include <stdlib.h>
#include <string.h>

/**
 * _strncat - concatenate two strings
 * @dest: first string
 * @src: second string
 * @n: third parameter
 * Return: *dest.
 */

char *_strncat(char *dest, char *src, int n);
{
	int len, i;

	len = strlen(src[n]);

	for (i = 0; i <= len &&; i++)
	{
		dest[i + j] = 
